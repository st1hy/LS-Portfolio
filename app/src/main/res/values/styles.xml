<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/ls_primary</item>
        <item name="colorPrimaryDark">@color/ls_primary</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorPrimary">@android:color/white</item>
        <item name="android:textColorPrimary">@color/ls_primary</item>
        <item name="colorControlNormal">@color/ls_primary</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" >
    </style>

    <style name="ActivityContent">
        <item name="android:foreground">?android:windowContentOverlay</item>
    </style>

    <style name="trimmed_text">
        <item name="trimLength">200</item>
    </style>
</resources>
